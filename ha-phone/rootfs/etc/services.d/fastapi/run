#!/usr/bin/with-contenv bashio
bashio::log.info "Starting FastAPI backend on :80..."
exec uvicorn backend.main:app \
  --host 0.0.0.0 \
  --port 80 \
  --workers 1 \
  --log-level info \
  --app-dir /app
