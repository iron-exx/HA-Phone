; HA-Phone — voicemail.conf
; Voicemail spool location controlled by astspooldir in asterisk.conf
; [general] is generated to /data (msmtp-aware, from the SMTP settings) so it can
; be updated at runtime without touching the image.
#include "/data/asterisk/voicemail_general.conf"
#include "/data/asterisk/voicemail_mailboxes.conf"
