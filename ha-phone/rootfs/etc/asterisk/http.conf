; hassio-bpx — http.conf
; ARI HTTP server bound to 127.0.0.1 ONLY (D-08, SEC-02)
; NOTE: ARI binding is here, NOT in ari.conf — ari.conf controls users only
[general]
enabled  = yes
bindaddr = 127.0.0.1
bindport = 8088
