; hassio-bpx — extensions.conf
; Phase 2: routing via included file from /data/asterisk/
[general]
static       = yes
writeprotect = no

[default]
; empty context — routing is in extensions_routing.conf

#include "/data/asterisk/extensions_routing.conf"
