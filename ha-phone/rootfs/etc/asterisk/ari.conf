; hassio-bpx — ari.conf
; ARI user config — network binding is in http.conf, not here
[general]
enabled = yes
; allowed_origins left unset in Phase 1 (no browser WebSocket yet)
; Set to * in Phase 3 for ingress proxy

; TODO Phase 3: generate password at runtime
[bpx-ari]
type            = user
read_only       = no
password        = changeme-ari-replace-in-phase3
password_format = plain
